# Example: Using Local Custom Provider
# Copy this file to 00-versions-local.tf and uncomment to use the custom-built provider
#
# After building and installing the provider (see BUILD_PROVIDER.md), update this file
# to use the local provider source.

# terraform {
#   required_version = ">= 1.5.0"
#
#   required_providers {
#     aws = {
#       source  = "hashicorp/aws"
#       version = ">= 6.0"
#     }
#     rhcs = {
#       # Use local provider with audit_log_arn support
#       # Version should match what was installed via 'make install'
#       source  = "terraform.local/local/rhcs"
#       version = "0.0.2"  # Update to match installed version
#     }
#     null = {
#       source  = "hashicorp/null"
#       version = ">= 3.0"
#     }
#   }
# }
